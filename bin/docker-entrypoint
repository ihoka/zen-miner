#!/bin/bash -e

# Strip leading "--" passed by Kamal for interactive commands
while [ "$1" = "--" ]; do
  shift
done

# Enable higher verbosity for troubleshooting
if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
  ./bin/rails db:prepare
fi

exec "${@}"
